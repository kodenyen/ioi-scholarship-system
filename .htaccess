<IfModule mod_rewrite.c>
  RewriteEngine on

  # Allow direct access to the uploads folder
  RewriteCond %{REQUEST_URI} ^/uploads [NC]
  RewriteRule ^(.*)$ $1 [L]

  # Route everything else to the public folder
  RewriteRule ^$ public/ [L]
  RewriteRule (.*) public/$1 [L]
</IfModule>
