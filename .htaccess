<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # If the request is already for the public folder, don't rewrite
  RewriteCond %{REQUEST_URI} !^/public/
  
  # Rewrite everything else to the public folder
  RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
